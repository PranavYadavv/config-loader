[project]
name = "config-loader"
version = "0.1.0"
description = "Universal configuration loader for YAML, JSON, TOML and env files"
requires-python = ">=3.8"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"
